export class SpatialGrid {constructor(cellSize=160){this.cellSize=cellSize;this.cells=new Map()}clear(){this.cells.clear()}key(x,y){return `${Math.floor(x/this.cellSize)},${Math.floor(y/this.cellSize)}`}insert(obj,x=obj.x,y=obj.y){const k=this.key(x,y);let a=this.cells.get(k);if(!a)this.cells.set(k,a=[]);a.push(obj)}rebuild(items,filter=null){this.clear();for(const o of items)if(!filter||filter(o))this.insert(o)}queryRadius(x,y,r){const out=[],seen=new Set(),cs=this.cellSize,minX=Math.floor((x-r)/cs),maxX=Math.floor((x+r)/cs),minY=Math.floor((y-r)/cs),maxY=Math.floor((y+r)/cs);for(let gx=minX;gx<=maxX;gx++)for(let gy=minY;gy<=maxY;gy++){const a=this.cells.get(`${gx},${gy}`);if(!a)continue;for(const o of a){if(seen.has(o))continue;seen.add(o);out.push(o)}}return out}nearest(x,y,r=1600,predicate=null){let best=null,bestD=r*r;for(const o of this.queryRadius(x,y,r)){if(predicate&&!predicate(o))continue;const d=(o.x-x)**2+(o.y-y)**2;if(d<bestD){bestD=d;best=o}}return best}}
